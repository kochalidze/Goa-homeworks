function evenNumbers(array, number) {
    let arr2 =  array.filter((el) => el % 2 === 0)
    arr2 = arr2.slice(-number)
    return arr2
}

function evenNumbers(array, number) {
    let arr2 =  array.filter((el) => el % 2 === 0)
    arr2 = arr2.slice(-number)
    return arr2
}